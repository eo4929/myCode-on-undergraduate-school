#include "stdio.h"
#include "stack"

#define STACK_SIZE 50

struct Data_array
{
	int *arr;
	int length;
};

std::stack<Data_array>

/*
typedef struct Stack
{
int arr[STACK_SIZE];
int top;
int isEmpty;
}Stack;

Stack stack;

void initStack()
{
int i;
for (i = 0; i < STACK_SIZE; i++)
{
stack.arr[i] = 0;
}
stack.isEmpty = 1;
stack.top = 0;
}

int isEmpty()
{
return stack.isEmpty;
}

void push(Stack* stack, int data)
{
stack->top++;
stack->arr[stack->top] = data;

if (stack->isEmpty == 1)
stack->isEmpty = 0;

}

int pop(Stack* stack)
{
int val = stack->arr[stack->top];
stack->top--;

if (isEmpty())
stack->isEmpty = 0;

return val;
}
*/



void main()
{

}